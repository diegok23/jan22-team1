import { createTheme } from "@mui/system";

 const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
          main: '#202020',
        },
        secondary: {
          main: '#15d4be',
        },
        white: {
          main: '#fff',
        },
      }, 
})

export default theme